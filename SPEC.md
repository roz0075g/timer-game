# Timer Game 仕様書

## 1. 共通仕様

- ゲーム時間は30分（1800秒）。
- 1秒ごとにタイマーを進める。
- 一時停止中は時間・ゲーム状態を進めない。
- リセットすると時間、抽選結果、累積回数、達成/失敗数、状態を初期化する。
- ゲーム開始後はモード変更できない。
- 6つの10秒枠を使用する。
  - 枠1: 0–9秒
  - 枠2: 10–19秒
  - 枠3: 20–29秒
  - 枠4: 30–39秒
  - 枠5: 40–49秒
  - 枠6: 50–59秒
- 抽選結果は1～6で、結果nは枠nを対象とする。
- 対象枠の累積回数が1増えるたび、その枠の今回ノルマは10ずつ増える。
- PENDING（処理待ち）に対して「達成」または「失敗」を1回だけ選択できる。
- 時間切れになったPENDINGは自動的に失敗扱いにする。

## 2. 通常モード

### フェーズ
1. 1分目、3分目、5分目…は抽選フェーズ。
2. 2分目、4分目、6分目…は実行フェーズ。
3. 抽選フェーズ開始時に1～6を1回抽選し、対応する枠の累積回数を1増やす。
4. 実行フェーズでは、その1分の10秒枠を順番に現在位置として進める。
5. 現在位置が進んだ時点で、前の10秒枠に残っているPENDINGは失敗扱いにする。
6. 実行フェーズ終了時、残ったPENDINGは失敗扱いにする。

### 現在位置
- 実行フェーズでは、経過秒から0～5の枠番号を計算する。
- UIでは実行フェーズの現在枠に「◀ 現在」を表示する。
- 抽選フェーズでは現在位置を実行枠として扱わない。

## 3. ハードモード

### 基本仕様
- 抽選フェーズを表示上・進行上なくす。
- 30分間、毎分すぐ実行フェーズにする。
- 各分の開始時に内部で1～6を1回抽選する。
- 抽選した枠の累積回数を1増やし、その枠だけをPENDINGにする。
- 抽選した枠を、その1分間の現在位置として固定する。
- 達成または失敗を選択しても、現在位置は変わらない。
- 次の分になったら、新しい抽選結果に応じて現在枠を切り替える。
- ハードモードでも、PENDINGのまま次の分へ持ち越さず、次の分開始時に残っていれば失敗扱いにする。
- 30分経過時は残っているPENDINGを失敗扱いにして終了する。

### ハードモードの現在位置
- 現在位置は「その分に内部抽選された対象枠」とする。
- 0～59秒の経過位置ではなく、対象枠そのものを現在位置として表示する。
- 達成/失敗後に0秒枠へ戻ることは禁止する。

## 4. UI仕様

- モード選択: 「通常」「ハード」。
- 選択中のモードを表示する。
- タイマー、現在フェーズ、直近の抽選結果、6枠の累積回数・今回ノルマ・状態を表示する。
- 実行フェーズの現在枠には「◀ 現在」を表示する。
- PENDINGの枠には「達成」「失敗」ボタンを表示する。
- 達成/失敗後は対応するボタンを無効化する。
- ゲーム終了時に達成数・失敗数を表示する。

## 5. テスト仕様

### T01 初期状態
- 起動直後は00:00、開始前、通常モード。
- 6枠すべて累積0、状態NONE。

### T02 モード切替
- 開始前に通常→ハード、ハード→通常を切り替えられる。
- 開始後は切り替えられない。
- リセット後は通常モードに戻る。

### T03 通常モード抽選
- 抽選フェーズ開始時に抽選が1回だけ行われる。
- 1～6の結果に対応する枠の累積回数が1増える。
- 同じ分の中で抽選回数が増えない。

### T04 通常モード現在位置
- 実行フェーズ開始時に現在位置が0～9秒枠になる。
- 10秒ごとに現在位置が次の枠へ進む。
- 前の枠のPENDINGは枠移動時に失敗になる。
- 実行フェーズ終了時に残ったPENDINGは失敗になる。

### T05 ハードモード開始
- 00:01付近で実行フェーズになる。
- 抽選フェーズは発生しない。
- 各分の開始時に内部抽選が1回だけ行われる。
- 抽選対象枠の累積回数が1増え、その枠がPENDINGになる。

### T06 ハードモード現在位置
- 現在位置は内部抽選された対象枠になる。
- 同じ分の間は現在位置が変化しない。
- 達成を押しても現在位置は変わらない。
- 失敗を押しても現在位置は変わらない。
- 次の分の開始時に新しい対象枠へ切り替わる。

### T07 ハードモードの未処理
- 前の分のPENDINGが残っている場合、次の分開始時に失敗になる。
- 新しい分の対象枠だけが新たなPENDINGになる。

### T08 一時停止・再開
- 一時停止すると経過時間が進まない。
- 再開すると停止した状態から続行する。
- 一時停止中に達成/失敗操作はできない。

### T09 リセット
- タイマーが00:00に戻る。
- モードが通常に戻る。
- 全枠の累積・状態が初期化される。
- 達成数・失敗数が0になる。
- 抽選結果が消える。

### T10 ゲーム終了
- 30:00で終了状態になる。
- タイマーが30:00を超えない。
- 実行中ではなくなる。
- 残ったPENDINGは失敗になる。
- 終了画面に達成数・失敗数を表示する。

## 6. 実装上の重要な不変条件

- `elapsedSeconds` は0～1800の範囲。
- `counts` は常に6要素。
- `statuses` は常に6要素。
- PENDINGでない枠の達成/失敗操作は状態を変更しない。
- 同一分の開始処理を複数回実行しても、同じ分に抽選を重複させない。
- ハードモードの `currentSlot` は、その分の対象枠と一致する。
- ハードモードで対象枠が達成/失敗になっても `currentSlot` を別枠へ変更しない。
